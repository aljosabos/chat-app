{
  "name": "chat-app",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npx tsc",
    "start": "node server/dist/index.js",
    "prepare": "husky",
    "lint": "cd client && npx eslint . && cd ../server && npx eslint ."
  },
  "version": "1.0.0",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  },
  "dependencies": {},
  "description": "",
  "lint-staged": {
    "*": "npm run lint"
  }
}
